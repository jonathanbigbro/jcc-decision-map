# 定阵导航

双击 `启动定阵导航.cmd`，保持服务器窗口打开，再访问 http://127.0.0.1:8765 。需要已安装 Python 3.10 或更高版本。

也可直接打开 index.html 使用点选和决策导图；截图文字识别和自动版本检查需要通过新版启动脚本运行。升级时先关闭之前的服务器窗口，再重新启动。

1. 点选已有英雄、装备、海克斯。英雄数量按总张数算，二星为3张、三星为9张。
2. 或上传截图 → 识别文字 → 勾选确认已拥有的候选 → 修改数量 → 加入开局。
3. 填写阶段、金币、血量与场面质量。查看推荐、缺失条件、同行影响和下一步。
4. 点击「查看全部12条路线」比较其他阵容，点击「决策逻辑」检查规则。
5. 切到「纹章 / 神器定阵」，选装备 → 看携带英雄与体系 → 查看配套装备、成型名单、缺失条件。支持按样本表现或已有开局排序。浏览不会添加装备，点击「我已拥有」才录入该件装备。

组合库核对于2026-09-23，收录21种纹章、24件神器、137条搭配，关联19个体系。装备图鉴中其他神器也可选择，但无样本时不做强度推断。组合统计是成型样本共现，不能当作开局获胜率；小样本、九五经济不足、其他纹章/神器未拿到均会提示。原构筑不含携带者的条目标明变体，不提供未经核实的替换方案。已录入、条件齐全的样本优选会影响主导图装备/纹章匹配，各维度仍受原上限约束。

截图识别仅识别可见文字并匹配图鉴，不会自动判断无文字头像、星级、归属。商店卡和海克斯选项需要自行区分。没有调用云端视觉模型，也不会上传截图。

页面打开时会检查数据源的当前版本，页面可见时每5分钟复查，后台停留超过5分钟后返回也会检查。可以点击「检查版本」手动刷新；为避免频繁请求，同一服务器15秒内的手动检查共用结果。网络失败会保留上次成功检查时间，不会把旧结果标为最新。

版本号来自金铲铲大数据当前赛季的版本字段，并非游戏官方推送；源站可能晚于官方公告。游戏版本变化、赛季变化或统计样本变化都会提示。版本检查不会替换阵容数据或清空已录入开局。

推荐仍使用2026-09-21抓取的S18自然之力18.2a静态快照，阵容库尚不自动刷新。12条阵容、65个去重英雄名称、137件装备、228个强化来自金铲铲大数据。阵容构筑、数据和规则说明可在页面查看；匹配分不是胜率，规则未通过实战回测。

来源：https://www.dataj.cc/comp ；https://news.17173.com/content/09162026/220018011.shtml 。图标为腾讯游戏素材；仅用于个人攻略研究。

本地OCR使用Tesseract.js与Tesseract.js-core，Apache-2.0；语言文件来自 `@tesseract.js-data/chi_sim` 与 `@tesseract.js-data/eng`。许可证见vendor文件夹，分发来源见 THIRD_PARTY_NOTICES.md。
